# 功能与导出验证

## 0.5.0 本地版：色彩与无损

- 新增 10 组真实编解码测试通过：8／10／16 位原生平面往返、三种遮挡外样本不变、双 PCM 音轨、Rec.601 → Rec.709 对照转换、有限／全范围标签、取消清理、变帧率保留及遮挡错位拒绝、预览与成片一致、奇数尺寸／未知标签、AAC 原包及预填充边界。
- 原有分析 8 项通过；导出 5 项通过，含短 4K60、1080p120、RGB 截取与旋转、10 位 ProRes／FFV1、AAC 调音量与降帧。Sony 相机实拍专项 1 项因未提供原始素材跳过，PCM 合成样本不能替代该实拍验收。
- 源码 Qt 界面与独立 EXE 均通过合成素材验收：导入不分析、默认无损及参数锁定、手动画框、色彩参数保存／重新打开、原生逐帧验证、16 位 RGB Rec.709 导出；原视频 SHA-256 未改变，正常退出。
- AAC 保留压缩包不等于任意封装下逐采样起止一致：本轮明确发现 MP4 → MKV 的预填充前缀差异，界面和说明均有提示。HDR／Log／校准显示管理尚未提供，不以 SDR 测试声称完整专业色彩管理。

命令：`python tests/verify_color.py`、`python tests/verify_studio_export.py`、`python tests/verify_analysis.py`；打包后 `视频一键打码工具.exe --verify-color <全新测试目录>`。本轮为本机 Windows 11 验证，尚未在第三台设备独立实测。


## 0.4.1 误遮挡修复

本轮更新了识别、候选复核和跟踪行为；下方 0.4.0 / 0.3.x 为历史记录。实现与验证说明见 [误遮挡修复](docs/FALSE_POSITIVE_FIX.md)。分析回归 8 项、导出回归 6 组通过，界面工作流保持导入后先预览。

0.4.1 独立 EXE 使用新的缓存目录，在仅有 Windows 系统 PATH、无 PYTHONHOME / PYTHONPATH 的条件下完成原片预览、显式分析、处理预览和实际导出，正常退出且 Qt 窗口完成销毁。DirectML / D3D11VA 实际运行；输出 1920×1080、50 fps、288 帧，PCM 解码采样相同。作者文案、默认保留原画和两张收款码检查通过。


## 0.4.0 更新验证

- 三组分析回归通过：CPU / GPU 解码断点前后像素一致；取消后恢复及完整缓存复用；缓存失效、尾块恢复、内存块数上限。
- 原片预览先于分析，开始按钮触发、项目重开不自动分析、手动画框、截取、旋转、参数修改和缓存复用通过界面测试。
- 六组导出回归继续通过，包含短 4K60 / 1080p120、10 位 SDR、无损 RGB 像素对比、PCM 裁切、AAC 和取消清理。
- 约 11 分钟循环素材确认续跑总帧数与完整解码一致，详细计时、设备与限制见 [性能记录](docs/PERFORMANCE.md)。
- Windows 原生 Qt 验收执行原片播放、显式分析、处理预览和导出，并检查程序正常退出。原生视频窗口需要先停止播放器再销毁；提前拆开 GPU 视频输出会造成关闭阻塞，此顺序已修正。
- 0.4.0 独立 EXE 在仅有 Windows 系统 PATH、无 PYTHONHOME / PYTHONPATH 的环境完成验收，退出码 0；DirectML / D3D11VA 实际启用，输出 1920×1080、50 fps、288 帧和相同 PCM 采样；两张收款码缩略图、放大图和原图保存再次通过校验。
- 本轮保持未检测到脸时默认原画、作者文案及收款码原字节。下面 0.3.x 节为历史验证记录，其中旧预览转码流程已被替换。


## 0.3.2 更新验证

- 产品更名为“视频一键打码工具”，欢迎页使用简洁的免费使用与人工复查提示。
- 两张作者授权的收款码按原始字节打包；缩略图、放大图本地解码均与原码相同，保存原图后的文件字节一致。没有实际付款。
- 重新通过 UI 工作流：导入分析、预览播放、音轨、截取、手动关键帧、旋转和项目保存/重开。
- 欢迎页截图只含本工具界面，可以公开；视频验收截图与个人素材继续排除在分发之外。
- 安装后的独立运行和实际导出结果另见仓库中的 INSTALLER_VERIFICATION.md。

## 0.3.1 更新验证

- 默认未检测到人脸时保留原画，整帧遮黑仍可选择。8 项功能测试重新通过，包含默认原画与主动选择遮黑的像素校验。
- 新增作者欢迎页、启动不再提示设置、常驻作者入口、问题反馈和支持入口。已检查作者及邮箱、链接目标和不再提示的保存；未实际发送邮件或提交反馈。
- UI 工作流重新通过，包含导入后默认保留画面、播放、截取、补码、旋转和项目保存/重开。
- 微信及支付宝静态收款码展示入口已实现，当前未配置真实收款码，未进行付款测试。
- 下列 0.3.0 导出质量测试描述为前一版本已有验证，不能视为 0.3.1 在新机器上的重新测试。

本机 Windows 11 x64，Python 3.12，CPU 软件编码。没有在另一台实体电脑或 Windows 10 实测。

## 源码测试

- `tests/verify.py`：8 项通过。移动人脸、四类遮挡、强度、手动范围、无脸回看、无声素材、VFR、旋转元数据、五官失败保底、取消与源片保护、总时长。
- `tests/verify_studio_export.py`：6 组通过。4K 60 fps 的 15 帧、1080p 120 fps 的 30 帧保持尺寸、帧率与帧数。这是 0.25 秒功能样本，不是长片性能基准。
- FFV1 截取 7 帧、旋转 90°、带运动手动框，成片解码后与预期处理 RGB 数组逐元素一致。
- 实拍相机素材 1920×1080 / 50 fps / 288 帧 / 5.76 秒：完整输出 PCM 解码采样全部相同；截取 1～3 秒 PCM 与原片对应区间全部相同。
- 10 位 SDR 导出 ProRes / FFV1 保留高位深，阻止直接降为 8 位；另测 AAC 音量、有限静音补齐、降帧率、取消、禁止高于源帧率。
- `tests/studio_ui_smoke.py`：导入分析、整脸/眼睛、播放帧推进、音频轨加载、逐帧、截取、手动关键帧、旋转、项目保存/重开。自动测试未用麦克风或人耳评价实际听感。
- 截图只用本程序 Qt widget 的 grab，不截用户其他窗口；实拍素材、截图、测试输出排除出所有交付包。

## 复现

在 `samples/portrait.jpg` 放置[官方测试图](https://storage.googleapis.com/mediapipe-assets/portrait.jpg)，它不在源码包中。随后运行：

```
python tests/verify.py
python tests/verify_studio_export.py
python tests/studio_ui_smoke.py
```

可设置 `FACEPRIVACY_ACCEPTANCE_VIDEO` 指向自己的测试片进行额外相机验证；不设置则跳过私有样片测试，UI 测试使用第一条命令生成的公开测试片。

打包程序的显式自检入口：

```
视频一键打码工具.exe --verify "测试视频路径" "新建的验证输出目录"
```

它本地导入、分析、预览、实际导出，写报告和本程序截图；PCM 音轨额外比对原声音采样。正常启动不调用。目录须为新目录，避免与旧成片重名。

## 边界

独立程序已在本机迁移到新的中文目录，清空 Python 相关环境变量，PATH 仅保留 Windows 目录，使用 Windows 原生 Qt 窗口完成导入、分析、带音轨播放和实际导出。成片保持 1920×1080、50 fps、288 帧、5.76 秒；PCM 解码采样与源片全部相同。此测试排除了对开发 Python 路径的依赖，仍不等于另一台实体电脑的测试。

打包阶段发现并修正了开发工具 PATH 中同名 ICU DLL 被误收集的问题；构建现在限定依赖搜索来源。预览刚加载时的待处理跳转位置也已修正，并通过独立程序播放检查。

未测小时级长片、持续 4K 高帧率负载、不同显卡加速、全部相机格式、多音轨或 HDR。短样片通过不代表全面识别准确率。压缩音轨直拷有音频包边界误差。

## 0.5.0 原生导出修复验证（2026-09-10）

- `tests/verify_color.py`：13 项通过。新增 H.264 与 10 位 HEVC 的 P/B 帧素材，在部分帧遮挡，独立解码原片核对所有未遮挡样本；人为注入遮挡外错误会被保存前校验拒绝。取消校验后不保留临时成片。
- `tests/verify_preview.py`：5 项通过。覆盖解码器复用、前后定位、同帧反复修改效果、原始帧缓存不被污染、资源释放、色彩缓存失效、旋转素材默认档及真实 Qt 项目往返。
- 原有分析 8 项、导出 6 项回归通过，包括短 4K60／1080p120、10 位和 Sony PCM 原音频／截取采样检查。源码 Qt 全流程烟雾测试通过。
- 本机横屏实拍原生导出保持 1920×1080、50 fps、288 帧，并完成全部 288 帧的独立原片对照；90° 旋转标记的实拍自动选择 RGB 无损，导出保持 1080×1920、50 fps、264 帧。
- 原生预览按原尺寸和位深计算效果。本机一段 1080p50 素材的 8 个跳帧前进位置，单帧处理中位数约 53 ms，修复前约 121 ms；这不是完整 GUI 帧率或其他硬件的性能保证。8 张画面均与分别打开素材的精确读取一致。

`--verify-color` 验收增加了 H.264 单帧遮挡后其余 59 帧保持原像素，以及带旋转标记素材自动选择 RGB 无损并保持竖屏预览。运行时均使用本地合成素材；私人相机视频和验收输出不入库。

修复后的独立 EXE 已在新的本机目录通过上述六组 `--verify-color` 验收并正常退出；测试时清除了 PYTHONHOME／PYTHONPATH，PATH 仅保留 Windows 系统目录，使用 Windows 原生 Qt 窗口。便携目录扫描不含视频、音频、项目、识别缓存或日志。公开 0.4.1 Release 未在本轮替换。

质量取舍：原生导出新增独立原片解码核对，可能增加导出时间。手动色彩转换播放仍走精确 FFmpeg 转换，可能低于源帧率；不会为流畅度跳过色彩计算。尚未覆盖数小时压力测试和另一台实体电脑。
