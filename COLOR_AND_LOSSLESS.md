# 色彩与无损（0.5.0 本地预览版）

导入只引用原视频，不转码、不覆盖。识别与屏幕预览可以使用低分辨率、8 位副本；正式视频导出重新读取原片。

## 日常操作

1. 导入后打开“导出”，查看原片位深、像素格式、原色、传递曲线、矩阵与电平范围。
2. 默认优先“源像素无损”。支持的原片保持自动色彩，直接导出 MKV；需要分享小文件可选 H.264。
3. 需要修正解释或转换时切换普通档或“RGB 无损编码”，设置输入色彩、有限／全范围，输出保留或转换至 Rec.709、Rec.601 NTSC／PAL。手动输入设置是解释素材，不会修改原片。
4. 色彩参数随项目保存。预览用于检查遮挡，尚无显示器 ICC、校准监看或 HDR 显示管理。

## 两种无损分别保证什么

| | 源像素无损 · FFV1 | RGB 无损编码 · FFV1 |
|---|---|---|
| 编码 | 无损 | 无损 |
| 未遮挡区域 | 保留解码后的原生 YUV／GBR 样本 | 保留经过 RGB 转换及处理后的样本 |
| 位深／色彩 | 原格式、原位深、已有标记；未知标记保留 | 高位深使用 16 位 RGB；按所选色彩转换并写入标签 |
| 截取、缩放、旋转、转换色彩 | 本版不开放；切换时恢复完整原片 | 可用；这些操作可能不可逆 |
| 音频 | 所有音轨直接封装，或移除 | 延续原有第一音轨导出策略 |
| 保存前验证 | 每帧像素哈希、帧数、时间戳、色彩标记；音轨压缩包内容及首末时间 | 尺寸、帧率、时长、色彩标记 |

原生无损支持逐行、方形像素、无旋转标记的平面 YUV 4:2:0／4:2:2／4:4:4（8、9、10、12、14、16 位）和 GBR（9、10、12、14、16 位，高位深为小端）。色度子采样的一个样本可能覆盖多个像素；遮挡边缘共用的色度样本也计入遮挡范围。效果本身必然改变被遮挡区域。

原生导出保留视频帧时间戳到 MKV 的毫秒精度。带遮挡的变帧率素材如与识别时间线不一致会停止，不会强行对齐。视频中途改变格式、色彩、尺寸或出现异常时间戳也会停止。所有导出都另存新文件；取消或校验失败删除临时成片。

这不是整个文件逐字节一致：容器、视频压缩码流会改变；不保证字幕、附件、章节及全部相机元数据。已经损失的原片信息不能恢复。PNG 截图是 8 位预览帧，不是高位深无损母版。

原音频保留编码包，不再压缩，但不承诺所有编码的逐样本起止边界。已验证双 PCM 音轨样本一致；AAC 从 MP4 转为 MKV 时，编码预填充的跳过标记可能不能原样保留。例如合成样本包含 1024 个预填充样本，输出保留其负时间戳和全部压缩包，直接解码 PCM 会多出这段前缀。界面对 AAC 提示此边界；尚不能宣称音视频全链路无损。

## 本版色彩边界

本版提供轻量 SDR 管理。Rec.601／709 转换实际计算像素，再写入一致标签，不只是更换标签。涉及原色／曲线转换的处理使用 FFmpeg 12 位 YUV 中间格式，随后输出 RGB；因此转换本身不承诺数学无损。ProRes HQ 也属于有损中间编码。

未完整标记的 SDR 在普通导出中对缺失项按 Rec.709 解释，范围默认按视频有限范围（RGB 为全范围）；界面明确提示，用户可以修正。相机 Log 不能仅靠容器标签可靠识别，尚无相机输入变换。标记为 PQ、HLG 或 BT.2020 的素材拒绝导出，不静默降为 SDR。暂不提供 ACES、LUT、色调映射或 ICC 显示转换。

## 便携性与验证

沿用 CPU／DirectML 识别方案，没有新增 AI 模型或 CUDA 要求。新增 PyAV 及其随包动态库用于原生像素读写，运行时无需另装 FFmpeg 或 Python。便携版须携带整个目录，不能只复制单个 EXE；识别缓存及偏好仍保存在本机用户目录。无损文件较大，编码及逐帧复核增加 CPU、磁盘和导出时间。

开发检查：`python tests/verify_color.py`。打包检查：`视频一键打码工具.exe --verify-color <全新测试目录>`，仅生成合成测试图案和音频，检查真实 Qt 导入、参数保存、无损导出及色彩转换，输出 JSON 报告和界面截图。公开的 0.4.1 联网安装清单未改变；本版先以本地便携包验证。
