# 做法、价值与下一步

## 0.5.0 本地版：色彩与原生像素路径

新增 `color_management.py` 读取结构化色彩信息，构建实际 SDR 转换与输出标签，并验证成片标签。`native_export.py` 使用 PyAV 直接修改原生 YUV／GBR 平面，复用识别几何和手动关键帧；预览也使用该平面渲染器。视频保存前重新解码核对逐帧像素哈希、帧数与时间戳；音频核对压缩包及首末时间。普通导出仍走 RGB 路径。

默认优先原生无损，完整原片、原尺寸、原色彩；截取、变换、普通压缩和色彩转换由用户选择其他导出档。仅支持 SDR，具体样本边界、AAC 预填充及容器限制见 [色彩与无损](COLOR_AND_LOSSLESS.md)。下方通用 RGB 流程为原有路径。

## 处理流程

```
原视频 → Qt 原片预览（导入不分析）
用户开始 → 统一时间线 → 按方案缩放 → YuNet / 可选第二模型
                                           ↓
                                 局部裁剪放大 → MediaPipe 五官
                                           ↓
                              相对坐标 + 需要回看的时间段
                                           ↓
原尺寸重新解码 → 共用遮挡渲染器 ← 手动框 / 关键帧 / 截取区间
                      ↓
                  旋转 / 尺寸 / 帧率
                      ↓
           编码 + 原音轨处理 → 校验 → 新成片
```

1. **先找脸，再找五官。** 整幅远景直接估计关键点容易漏掉中小人脸。均衡模式的 YuNet 查看两种尺寸，合并重叠候选，再裁成人脸局部大图给 MediaPipe。细致模式增加镜像、第三尺度和已有任务包中的 BlazeFace 检测；快速模式省去高密度五官定位，间隔检测加光流。它改善了这次样片的中远景正脸，不代表在所有素材中都能达到同样准确率。
2. **五官失败先确认候选。** 对置信度较低或只由新增检测分支发现的候选补做验证，防止把手、肩膀或背景直接变成遮挡。通过验证后才用检测框近似轮廓；未通过的候选标记人工复查。近似框不额外叠加默认放大，五官成功时不再与大椭圆合并。
3. **遮挡几何独立于像素大小。** 轮廓、眼角、手动框使用 0～1 相对坐标。眼睛连线构造局部坐标系，整脸、上下半脸、眼部横条随倾斜变化。预览与导出共用渲染，参数改变不用重做检测。
4. **预览与成片分工。** 原片走 Qt 原生播放，处理预览把普通图像交给后台最新帧队列，成片重新读取原视频。缓存只存几何，每 180 帧事务提交，最多缓存 6 块；完成结果可以跨启动复用，部分结果可以续跑。模糊和马赛克只计算遮挡附近区域，但有损编码仍影响整幅输出画面。
5. **失败可以被检查。** 无脸、数量减少、五官失败形成时间线提示，默认无脸保留画面，整帧遮黑可自行选择。提示不等于完整隐私保证，仍需回看和补码。
6. **先校验，再保存。** 临时文件接收编码，检查进程成功、时长、尺寸、帧率、源文件未变后改名。音频补齐有明确上限。取消或失败清理半成品；原片不覆盖。

## 是否独到

这是成熟开源组件的工程组合，没有发明新模型，也没有证据称为领先算法。价值在于把采访需求做成闭环：中远景识别改进、几何可调、原尺寸导出、失败提示、手动关键帧补救，以及不依赖会员和云端。

识别负责“在哪里遮”，渲染负责“怎样遮”，导出负责尺寸、帧率和声音。分离之后，换 Qt 界面、加截取和旋转不用重写识别。将来简单图形、字幕或局部效果可复用帧渲染入口；复杂多轨剪辑、调色、变速仍需新的时间线和颜色模型。

| 模块 | 职责 |
| --- | --- |
| core.py | 统一时间线解码、几何与渲染，兼容旧检测和导出 |
| analysis_engine.py / analysis_cache.py | 三档识别、短时光流、SQLite 分块缓存与续跑 |
| acceleration.py | DirectML / CPU 实测选择、DXGI 枚举、硬件编解码探测 |
| playback.py / vision_runtime.py | 最新帧后台渲染、只加载视觉用途的 MediaPipe |
| exporter.py | 画质预设、声音、截取、旋转、位深、输出校验 |
| studio.py | Qt 界面、带声音预览、素材、参数、项目和队列 |
| acceptance.py | 显式命令启动的打包验收，正常启动不调用 |
| tests/ | 识别、成片与界面工作流验证 |
| Studio.spec / package_studio.py | 独立程序、便携包、源码整理包 |

## 本轮优化的边界

自动推理选择比较同尺寸 CPU / GPU 的短测，避免仅凭显卡名称决定；五官定位、光流和像素处理仍在 CPU。硬件解码与编码先实际启动探测，不能把 FFmpeg 列出的编码器当作可用证明。默认仍是画质优先的 CPU 编码。

短时跟踪检查前后向光流误差、几何尺度、位移与镜头变化；只在面部内选取光流点；完整检测失败立即停止延续，避免镜头切换后跟到手或身体。0.4.1 取消完整检测失效后的前向补偿；只在快速模式的检测间帧使用光流，完整检测结果优先。它不识别人是谁、不提供跨镜头身份关联，也不能可靠覆盖纯背头；续跑边界会重新建立短时跟踪，建议复查。

几何与时间线解耦是工程上的主要价值，不是新发明的模型。导入不扫描全片、预览不排长队、缓存分块落盘，让用户可以控制何时花算力。具体性能与不确定性见 [性能记录](docs/PERFORMANCE.md)。

仍值得做：更稳定的头部跟踪、指定人物、GPU 内完成缩放与遮挡以减少传输、数小时真实片压力测试、多轨音频、受控 HDR、撤销与自动恢复。最新 AI 候选及实施顺序单独放在 [讨论方案](docs/AI_PROPOSAL.md)，尚未接入程序。
