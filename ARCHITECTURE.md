# 做法、价值与下一步

## 处理流程

```
原视频 → 统一时间线 → 轻量逐帧画面 → 多尺度 YuNet 找脸
                                           ↓
                                 局部裁剪放大 → MediaPipe 五官
                                           ↓
                              相对坐标 + 需要回看的时间段
                                           ↓
原尺寸重新解码 → 共用遮挡渲染器 ← 手动框 / 关键帧 / 截取区间
                      ↓
                  旋转 / 尺寸 / 帧率
                      ↓
           编码 + 原音轨处理 → 校验 → 新成片
```

1. **先找脸，再找五官。** 整幅远景直接估计关键点容易漏掉中小人脸。YuNet 查看两种尺寸，合并重叠候选，再裁成人脸局部大图给 MediaPipe。它改善了这次样片的中远景正脸，不代表在所有素材中都能达到同样准确率。
2. **五官失败仍保留遮挡。** 用已检测的人脸框近似轮廓，利用眼睛位置估计方向，标记回看；避免因为关键点失败而直接漏掉已经找到的脸。近似框可能不够贴合。
3. **遮挡几何独立于像素大小。** 轮廓、眼角、手动框使用 0～1 相对坐标。眼睛连线构造局部坐标系，整脸、上下半脸、眼部横条随倾斜变化。预览与导出共用渲染，参数改变不用重做检测。
4. **预览与成片分工。** 轻量缓存负责流畅定位，成片重新读取原视频。模糊和马赛克只计算遮挡附近区域，但有损编码仍影响整幅输出画面。
5. **失败可以被检查。** 无脸、数量减少、五官失败形成时间线提示，默认无脸保留画面，整帧遮黑可自行选择。提示不等于完整隐私保证，仍需回看和补码。
6. **先校验，再保存。** 临时文件接收编码，检查进程成功、时长、尺寸、帧率、源文件未变后改名。音频补齐有明确上限。取消或失败清理半成品；原片不覆盖。

## 是否独到

这是成熟开源组件的工程组合，没有发明新模型，也没有证据称为领先算法。价值在于把采访需求做成闭环：中远景识别改进、几何可调、原尺寸导出、失败提示、手动关键帧补救，以及不依赖会员和云端。

识别负责“在哪里遮”，渲染负责“怎样遮”，导出负责尺寸、帧率和声音。分离之后，换 Qt 界面、加截取和旋转不用重写识别。将来简单图形、字幕或局部效果可复用帧渲染入口；复杂多轨剪辑、调色、变速仍需新的时间线和颜色模型。

| 模块 | 职责 |
| --- | --- |
| core.py | 解码、识别、几何、渲染、分析缓存，兼容旧导出 |
| exporter.py | 画质预设、声音、截取、旋转、位深、输出校验 |
| studio.py | Qt 界面、带声音预览、素材、参数、项目和队列 |
| acceptance.py | 显式命令启动的打包验收，正常启动不调用 |
| tests/ | 识别、成片与界面工作流验证 |
| Studio.spec / package_studio.py | 独立程序、便携包、源码整理包 |

## 下一步优先级

1. **轨迹与漏检补偿。** 稳定人脸轨迹，短暂漏检时运动预测，平滑抖动，镜头切换重置，防止遮挡漂移。这比单纯加大马赛克更有价值。
2. **选择人物。** 指定遮谁、保留谁，需要稳定轨迹和可回看的选择状态，不能简单依赖每帧人脸序号。
3. **长素材性能。** 检测与跟踪混合、分析分块写盘、复用解码器、后台队列和断点恢复。当前逐帧分析便于验证，但等待与内存仍需改善。
4. **高质量导出速度。** 实测 AMD / Intel / NVIDIA 编解码，减少 CPU/GPU 搬运。仅看到编码器名称不代表能运行；同码率硬件编码也不保证画质更好。
5. **色彩与声音。** 色彩信息解析、10 位渐变专项对比、受控 HDR、多音轨、电平与响度。当前明确面向普通 SDR。
6. **日常编辑。** 撤销重做、自动恢复、时间线缩放、关键帧曲线、可保存预设、可暂停队列，优先于完整多轨剪辑。

高质量不是原片无损，高帧率兼容不是实时导出，关键帧插值不是自动追踪。实测边界见 `STUDIO_VERIFICATION.md`。
